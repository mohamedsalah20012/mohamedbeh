on:
  push:
    branches:
      - master


jobs:
  build:
    runs-on: ubuntu-latest 

    steps:
      - name: 'test yml'
        run: pwd 

      - name: 'test print'  
        run : echo moelserougy 

    